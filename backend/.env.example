# Environment Configuration for Backend API
# Copy this to .env (already configured with working values)

# Server Configuration
FLASK_ENV=development
FLASK_DEBUG=True
HOST=127.0.0.1
PORT=5000

# Secret Keys (Already configured - no need to change for development)
SECRET_KEY=sentra-dev-secret-key-2025
JWT_SECRET_KEY=sentra-jwt-secret-key-2025

# MongoDB Configuration (Pre-configured with MongoDB Atlas - works out of the box!)
MONGO_URI=mongodb+srv://SentraAdmin:L1i1OunmTvpcolAM@sentraauth.onrz4g3.mongodb.net/sentra?retryWrites=true&w=majority&appName=SentraAuth
MONGO_DB_NAME=sentra_encryption
MONGO_COLLECTION_USERS=users
MONGO_COLLECTION_FILES=encrypted_files
MONGO_COLLECTION_RECIPIENTS=recipients

# File Storage
UPLOAD_FOLDER=./data/uploads
ENCRYPTED_FOLDER=./data/encrypted
DECRYPTED_FOLDER=./data/decrypted
KEYS_FOLDER=./data/keys
METADATA_FOLDER=./data/metadata
TEMP_FOLDER=./data/temp
MAX_FILE_SIZE=104857600  # 100MB in bytes

# Security
BCRYPT_ROUNDS=12
TOKEN_EXPIRY_HOURS=24
REFRESH_TOKEN_EXPIRY_DAYS=30

# CORS
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# AI Sensitivity Analysis (Optional)
AI_ANALYSIS_ENABLED=true
AI_API_KEY=your-ai-api-key-if-needed

# Logging
LOG_LEVEL=INFO
LOG_FILE=./logs/app.log
